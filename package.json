{
  "name": "kirana-dai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first 3D foundation for Kirana Dai — Nepali Kirana Shop Simulator",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.0",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
